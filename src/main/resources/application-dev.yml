spring:
  datasource:
    url: jdbc:mysql://localhost:3306/projectmg_dev
    username: root
    password: "!Little2410!"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: always

  flyway:
    enabled: false

security:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000

server:
  tomcat:
    use-native: false
